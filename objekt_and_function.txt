обьект (как любая вещь из реального мира,является изменяемой) - машина
свойства(если меняем их, обьект остается тот же самый) - красная,текущая скорость 0 км\ч,макс. скорость 200 км\ч,топливо: бензин
функции (функция какого-то обьекта, глагол) - может двигаться,можно вкл.фары
---------------
let car = {
    prop(свойства):value(значение)
};

let car = {
    color:'red'   (создаем обьект car со свойством red)
};
----------------

let car = {
    color:'red',
    maxSpeed:250
};
console.log(car);(выводим обьект в консоль, 
она покажет обьект со свойствами цвет и макс.скорость)
console.log(car.color); (выводим свойство обьекта в консоль)

car.color = 'green';(можно изменить свойство обьекта, такой метод предпочтительнее) 
car['color'] = 'green';( можно и так изменить свойство обьекта)
let carProp = 'color' (можно создать переменную, в которой будет храниться свойство обьекта)
-----------
let car = {
    color:'red',
    maxSpeed:250
    audio:{
        brand:'sony',
        dinamic:4
    };
};
console.log(car);  (делаем обьект в обьекте и выводим в консоль)

console.log(car.audio.brand); (выводим свойство второго обьекта в консоль)













-----------
 if (0 === weight % 2)  ( если остаток от деления(%) веса(weight) на 2 равен нулю(0 ===)) то бла  бла

function(ключевое слово, зарезервировано яп)) divide(название функции)(weight(аргумент){
     if (weight === 2){
       return false;
    }
    if (0 === weight % 2) {
      return true;
    }
    if (1 == weight % 2) {
      return false;
    }
    
  }

console.log(divide(2)); ( вывод в консоль) ( если будет 2, то фукция дальше первого условия не пойдет)

return - это выход из функции , если return выполняется, то следующий код игнорируется
-----------


