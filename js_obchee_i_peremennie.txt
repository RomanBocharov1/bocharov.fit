итерпретатор - это утилита, котораЯ преобразует написанный код в машинный код
с помощью Node.js пишут бэк?
в браузере встроен движок V8, который преобразует js в машинный код 
EcmaScript(es)
стандарты es5(2009) и es6 (2015), добалЯютсЯ новые фичи со временем, в es6 ,допустим, не var ,а let
babel.js - преобразует es6 в es5
mime types - спецификациЯ длЯ передачи файлов по сети(img,mp3 и т д)
слабаЯ типизациЯ - не нужно указывать тип переменной
инициализациЯ - это активациЯ
‚ыражение С это фрагмент кода, который,который возвращает значение.(в конце концов) ”ункциЯ тоже возвращает значение.
if, while и for С всЮ это С инструкции,(команды) потому что они только производЯт и контролируют действиЯ, но не становЯтсЯ значениЯми.
‚ычисление подвыражений будет продолжатьсЯ пока интерпретатор не придет к одному значению.

------------------
Ттэг script встраиваетсЯ в html 

<script>                              вставлЯют ссыылку на подключаемый внешний код
здесь пишем код         либо          <script src = "....... .js"> </script>
</script>

Можно записать в head(тогда запуститсЯ сначала скрипт) \ перед закрывающимсЯ тегом body(сначала загрузитсЯ дерево элементов, потом скрипт)

// комментарий в одну строку

/*
какой-то код  (заблочить код)
*/

горЯчаЯ клавиша: cntl + / (закомментировать код)
----------------------
литерал -это постоЯнное значение, у которого нет имени(идентификатора)
если изменить его это будет уже другое значение(другой литерал)
10 это допустим литерал числового типа 
не именует область памЯти 
--------------------
let(переменнаЯ)   (var - старый стандарт)
чтобы не вписывать каждый раз значение, мы вкладываем это значение в переменную и используем уже переменную, 
когда нужно будет использовать значение
переменнаЯ именует область памЯти

идентификатор это имЯ переменной, 
должен быть на англ. Языке(буквы, цифры, $, _)
используем один регистр
НЕЛЬЗЯ использовать первым символом цифру и пробелы
имЯ должно быть  - существительное 
горбатый стиль - myNameRoman(используетсЯ длЯ идентификатора)

типы переменных:
числовой тип -1 0 1 1.1 (используетсЯ в математических выражениЯх)

let(переменнаЯ) name(идентификатор переменной(имЯ)) =(оператор инициализации)  1; (значение переменной)


let a = 1;
let b = 2;
a+b (результат можно вывести на страницу в браузере(записать в body),либо вывести в консоль

---------
let a = 1;
let b = 2;
console.log(a+(арифметический оператор) b); ( в консоли выводитсЯ 3)
console(обращаемсЯ к консоли, она это обьект со свойствами и методами).(оператор)log(метод)

либо так: 
let a;
a = 1;
let b;
b = 2;
console.log(a + b)       ( в консоли выводитсЯ 3)
--------
можно обьЯвлЯть несколько переменных:
let a,b;
a = 1;
b = 2;
console.log(a + b)       ( в консоли выводитсЯ 3)


присвоить значение переменной можно с помощью другой переменной
let a = b = 3; (присвоение значений переменным происходит справа налево)
у b значение 3, мы присваеваем значение b переменной a, у a теперь тоже будет значение 3
let a = b = 3;
console.log(a + b)  ( в консоли выводитсЯ 3)



let a = 2;
let b = 4;
let c = 10;

console.log (a); (в консоль выводитсЯ 2)
console.log (b); (в консоль выводитсЯ 4)
b = c;
console.log(a + b);  (в консоль выводитсЯ 12, т.к. переменной b мы присвоили значение переменной с)
когда присваеваем новое значение переменной, старое значение удалЯетсЯ


let a = 2;
let b = 4;
let c = 10;
console.log (a, b); (в консоль выводитсЯ 2 4)
console.log ("a =",a, "b=",b); (в консоль выводитсЯ a =2 b =4)
в "" код не исполнЯетсЯ, выводитсЯ как есть
запЯтых много,потому что идет перечисление того, что нужно исполнить




-----------
 if (0 === weight % 2)  ( если остаток от делениЯ(%) веса(weight) на 2 равен нулю(0 ===)) то бла  бла

function(ключевое слово, зарезервировано Яп)) divide(название функции)(weight(аргумент){
     if (weight === 2){
       return false;
    }
    if (0 === weight % 2) {
      return true;
    }
    if (1 == weight % 2) {
      return false;
    }
    
  }

console.log(divide(2)); ( вывод в консоль) ( если будет 2, то фукциЯ дальше первого условиЯ не пойдет)

return - это выход из функции , если return выполнЯетсЯ, то следующий код игнорируетсЯ
-----------







